# Pinned to 2.3.1 for the bundled free relay (2.3.2's newer handshake isn't
# spoken by the minimal relay). If you use AltTester Desktop instead, 2.3.2 is fine.
AltTester-Driver==2.3.1
pytest~=8.3.0
pytest-html~=4.1.0
Appium-Python-Client~=4.2.0
websockets>=12
